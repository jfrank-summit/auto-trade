# Environment variables for Auto-Trade

# -- Data Stores --
# Redis connection URL (e.g., redis://localhost:6379 or redis://user:password@host:port)
REDIS_URL=redis://localhost:6379

# PostgreSQL connection URL (e.g., postgresql://user:password@host:port/database)
POSTGRES_URL=postgresql://autotrade_user:yoursecurepassword@localhost:5432/autotrade_dev

# -- Data Ingestion Service --
# The GraphQL endpoint for the target DEX subgraph (e.g., Aerodrome on Base)
TARGET_DEX_SUBGRAPH_URL=https://api.thegraph.com/subgraphs/name/your-target-subgraph-here

# Optional: Polling interval for fetching new data in milliseconds
# POLLING_INTERVAL_MS=60000

# Optional: Batch size for processing trades from Redis to PostgreSQL
# PERSISTENCE_BATCH_SIZE=100

# -- Auto-Drive (Future Archival Integration) --
# AUTO_DRIVE_API_KEY=your-autodrive-api-key-here

# -- Ethereum Node (Future Live Trading / Direct Chain Interaction) --
# ETH_RPC_URL=https://your-eth-node-rpc-url.com/your-api-key

# -- Logging --
# Optional: Log level (e.g., info, debug, warn, error)
# LOG_LEVEL=info 